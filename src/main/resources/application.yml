server:
  port: 7575
# =======================================
# = Spring Configuration
# =======================================
spring:
  application:
    name: backend-api
  datasource:
    url: jdbc:postgresql://localhost:5432/lunchtag
    username: postgres
    password: 123456789
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
